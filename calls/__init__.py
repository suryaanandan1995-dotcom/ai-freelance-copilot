"""Booked-call detection and briefing.

The owner reads email once a day and never opens a dashboard, so a booked call has to
arrive as an email that says who booked, why they probably booked, and how to run the
15 minutes. See :mod:`calls.detect`.
"""
