# Core
pydantic>=2.6
pydantic-settings>=2.2
SQLAlchemy>=2.0
python-dotenv>=1.0

# Claude API + agents
anthropic>=0.40
langgraph>=0.2
langchain-core>=0.3
langchain-anthropic>=0.2

# Sources / web
feedparser>=6.0
httpx>=0.27
requests>=2.31

# Deliverability. NOT optional in practice, despite outreach.extract's ImportError
# fallback: without dnspython that fallback is socket.getaddrinfo, which resolves A
# records only and therefore reports "cannot receive mail" for every MX-only domain.
# Measured on live employer domains: agilebits.com publishes 7 MX records and no A
# record, so the fallback silently dropped a contactable lead and the funnel reported
# it as uncontactable — a wrong number, not a missing one. 1 of 24 domains checked.
dnspython>=2.6

# API + dashboard
fastapi>=0.110
uvicorn>=0.29
jinja2>=3.1

# MCP server
mcp>=1.2

# Observability
prometheus-client>=0.20

# UX / dev
rich>=13.7
pytest>=8.0
ruff>=0.5

# Optional production extras (installed via `pip install .[prod]`)
#   psycopg2-binary  -> PostgreSQL
#   qdrant-client    -> external vector store
#   sentence-transformers -> real embeddings
