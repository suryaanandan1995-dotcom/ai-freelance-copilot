apiVersion: v1
kind: Service
metadata:
  name: ai-freelance-copilot
  labels:
    app: ai-freelance-copilot
spec:
  type: ClusterIP
  selector:
    app: ai-freelance-copilot
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
